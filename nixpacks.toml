[variables]
SKIP_CLIENT_INSTALL = "true"

[phases.setup]
nixPkgs = ["nodejs_18"]

[phases.install]
# Skip postinstall script - SKIP_CLIENT_INSTALL env var is set above

[phases.build]
# No build step needed for worker

[start]
cmd = "node server/worker.js"
