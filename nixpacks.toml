[phases.setup]
nixPkgs = ["nodejs_18"]

[phases.install]
# Skip postinstall script by setting SKIP_CLIENT_INSTALL env var
cmds = ["SKIP_CLIENT_INSTALL=true npm ci --only=production"]

[phases.build]
# No build step needed for worker

[start]
cmd = "node server/worker.js"
